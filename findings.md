# Figure 6 光谱图复现发现

## 已知需求

- `Paper/Plot/figure6/data.csv` 第一列为波长。
- 第二列为 sum 强度，绘制蓝色曲线。
- CSV 另有 Eu II 数据列，绘制红色曲线。
- 不绘制参考图中含义未说明的橙色曲线。
- 脚本顶部需有集中式 `DEFAULTS`，便于调整输入输出、画布、DPI、范围、颜色、线宽、平滑、网格、字体、刻度、边框、图例和标签等。

## 工作树

- 仓库在开始时已经存在多项已修改及未跟踪内容，均视为用户已有工作，不触碰。
- `Paper/Plot/figure6/` 在检查时已有 `plot_figure6.py`、`test_plot_figure6.py`、`figure6_spectrum.png` 和 `repro_example1/`；这些文件可能是用户已有尝试，实施前需先检查，避免直接覆盖。

## 环境

- 默认 `python` 为 Python 3.7.0。
- 未安装 Windows `py` 启动器。
- 已安装 NumPy 1.21.6、Matplotlib 2.2.3、SciPy 1.1.0。

## 数据与参考图

- CSV 表头第一列为 `Wavelength (nm)`，第二列为 `Sum(calc)`，目标红线列为 `Eu II (8.6e-3)`；需用规范化列名匹配 `Eu II`，而不是写死括号里的浓度后缀。
- CSV 很大（约 4.4 MB），包含大量其他元素/电离态列；绘图只选波长、Sum 和 Eu II。
- 开始时 `data.csv` SHA-256 为 `44B20EBCAB9103C42A87AB04AAECC8F933CFBF8863421C19C403ED74F5EC2B7D`。
- 参考图分辨率为 1253×768；白底，蓝色总强度曲线与红色 Eu II 曲线，无图例、无轴标签、无纵轴数字与边框，主/次浅灰网格，横轴约 379–385.2 nm。
- 参考图底部还有橙线，但按用户要求不绘制。
- 参考图和默认输出的实际尺寸均为 1253×800 像素（最初从聊天预览估计的 1253×768 仅是显示缩放，不是源文件尺寸）。

## 现有同目录文件

- 已有 `plot_figure6.py`/`test_plot_figure6.py` 是全谱绘制与峰标注用途，默认范围和目标与本任务不同。
- 已有 `repro_example1/` 是同一目标的隔离复现尝试；本任务保留其结构，在该目录内补齐新增的集中配置要求和回归测试，不覆盖旁边的全谱绘图文件。

## 视觉与量化核对

- 默认输出只包含蓝色 Sum 和红色 Eu II；参考图中的橙色 Tm II 按要求移除。
- 视觉检查显示峰位、峰形、线色、网格、刻度位置和裁切与参考图一致。
- `compare.py` 的 8/8 检查通过：画布尺寸、蓝线像素数、无橙色像素、蓝/红中心线误差、主/次网格、横轴标签和白色背景均通过。
- 蓝线中心 RMSE 0.578 px，红线中心 RMSE 0.435 px；网格中心最大偏差 1 px；标签中心最大偏差 1.92 px。
- 生成后 `data.csv` SHA-256 仍为 `44B20EBCAB9103C42A87AB04AAECC8F933CFBF8863421C19C403ED74F5EC2B7D`。

## 独立评审修复

- 旧 compare 只在共同列计算中心误差，红线仅剩一列或一半仍可能通过；现在要求参考列覆盖率至少 95%、共同列至少 100，并限制 repro-only 比例。默认红线额外列较多是因为参考图的橙线遮挡红色基线，故红线阈值依据实测设为 58%，默认值为 55.8%。
- 网格原先是可复用同一候选的最近邻匹配；一维网格现在排序后一一配对。
- compare 是针对固定参考画布的测量器，尺寸不符时现在立即完整 FAIL，避免固定 spot 坐标越界。
- CSV 列严格由 `wavelength_column` 与 `curves.*.column` 指定，不模糊猜测 Eu 列；在创建 figure 前检查列、有限数值、严格递增/重复、窗口非空及至少 2 点。
- `reference_canvas_px=(1253, 800)` 明确 `axis_bottom_px` 的参考坐标语义，axes 矩形使用归一化比例，所以改变 figsize/DPI 不再改变相对布局。
- axes 背景、axisbelow、网格 linestyle/alpha/antialias，以及曲线 antialias/capstyle/joinstyle 均显式集中，默认值等于原 Matplotlib 渲染值。
- 输出路径只接受 `.png`（大小写不敏感），保存显式传递 `format="png"`；验证在异常时不会创建误导文件或目录。
