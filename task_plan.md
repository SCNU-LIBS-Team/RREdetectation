# Figure 6 光谱图复现计划

## 目标

在不修改 `Paper/Plot/figure6/data.csv` 的前提下，提供可重复运行的 Python 绘图脚本和高分辨率 PNG。第一列波长、第二列 sum 绘为蓝线，Eu II 数据列绘为红线；所有常调参数集中在脚本顶部 `DEFAULTS`。

## 阶段

### Phase 1: 检查与记录

- **Status:** complete
   - 检查工作树、数据结构、现有同目录文件和参考图。
   - 记录已有用户改动，避免覆盖。

### Phase 2: 测试先行与实现

- **Status:** complete
   - 先添加针对数据识别、配置集中化和绘图输出的测试。
   - 运行测试确认因生产模块不存在/功能缺失而失败。
   - 添加最小实现并让测试通过。

### Phase 3: 生成、视觉核对与最终验证

- **Status:** complete
   - 运行脚本生成高分辨率 PNG。
   - 对参考图和生成图进行视觉检查。
   - 运行完整测试和脚本验证，核对输入文件未改变。

独立评审发现了与本任务直接相关的正确性缺口，因此重新开启任务并增加以下阶段。

### Phase 4: compare.py 安全门禁

- **Status:** complete
   - 先以反例测试复现低参考覆盖率、额外曲线、网格重复匹配和过小画布问题。
   - 增加覆盖率/额外列门槛、一一对应网格匹配和安全尺寸处理。

### Phase 5: CSV 验证、配置和缩放语义

- **Status:** complete
   - 先测试所需列、有限数值、严格递增、重复、空窗口和点数不足。
   - 把 wavelength/曲线列选择、参考画布、axes 背景和抗锯齿/端点/连接样式纳入 `DEFAULTS`。
   - 修复 figsize/dpi 改动时 axis bottom 的比例布局。

### Phase 6: PNG 集成行为与文档

- **Status:** complete
   - 先测试真实 PNG、尺寸、目录创建、非 PNG 拒绝和输入内容不变。
   - 强制 `format="png"`，修正文档中的深合并与 `--show` 描述。

### Phase 7: 回归、量化比较与视觉复核

- **Status:** in_progress
   - 运行全部测试、`py_compile`、默认生成、compare 和输入哈希核对。
   - 人工检查默认视觉结果没有改变。

## 范围约束

- 不修改 `data.csv`。
- 不绘制未说明的橙色曲线。
- 不提交、不推送。
- 不触碰无关的已有修改或未跟踪文件。

## 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| `session-catchup.py` 在 Python 3.7 下因海象运算符 `:=` 语法失败 | 1 | 检查可用 Python；系统无 `py` 启动器，记录后继续人工检查工作树与计划文件。 |
| PowerShell `Get-ChildItem -Filter` 不接受数组 | 1 | 不再重复；改用显式路径检查或 `rg --files`。 |
| 首次临时统计命令错误地用 pandas 标签索引读取 `argmax()` 位置，触发 `KeyError: 65` | 1 | 不重复该写法；若需统计改用 `idxmax()` 或 NumPy 位置索引。 |
| 在 `repro_example1/` 工作目录用相对路径读取根目录 `task_plan.md`，路径不存在 | 1 | 其余生成与比较步骤未受影响；后续从仓库根读取计划文件。 |
| 一次 `rg` 正则在 PowerShell/JavaScript 双层转义后成为无效表达式 | 1 | 不重复复杂正则；最终核对使用多个简单 `rg -n -e` 模式。 |
| `check-complete.ps1` 首次报告 0/0，因为自定义计划未使用模板要求的阶段标题/状态格式 | 1 | 调整计划标题与状态格式后重新检查。 |
| 网格一一对应反例的第一次参数方向写反，旧算法也返回 99，测试意外通过 | 1 | 交换 repro/reference 列表方向，使旧算法错误复用同一中心并返回 1，测试能正确进入 RED。 |
| “额外红列”测试首次在被陡峭段过滤的列上叠加单像素，过滤结果没有变化 | 1 | 选取参考和复现均无中心的列，先清除该列已有红像素再植入独立红点，使反例确实增加 repro-only 中心。 |
